import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  image: {
    alignContent: 'center',
    alignSelf: 'center',
    flex: 1,
    height: 50,
  },
});

export default styles;
