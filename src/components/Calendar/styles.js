import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  container: {
    padding: 10,
    margin: 15,
    borderRadius: 15,
    marginTop: -15,
    height: 100,
  },
});

export default styles;
